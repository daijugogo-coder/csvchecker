# CSV Checker (Streamlit)

## Overview
Upload a CSV file via browser and validate it with predefined business rules.
Files are processed in memory and never stored on the server.

## Security
- Static analysis: Bandit (no issues found)
- Dependency audit: pip-audit (no known vulnerabilities)
- SBOM: CycloneDX (`sbom.json`)
